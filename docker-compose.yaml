version: "3.9"
services:
        elonbot:
                build: .
                volumes:
                        - ./log:/usr/src/elonbot/log:z
                        - ./auth:/usr/src/elonbot/auth:ro
                        - /etc/localtime:/etc/localtime:ro
                restart: always
                init: true
                cap_add:
                        - SYS_ADMIN
